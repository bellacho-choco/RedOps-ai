fastapi>=0.110
uvicorn>=0.29
pydantic>=2.7
httpx>=0.27
# optional (flagship features degrade gracefully when absent)
# docker>=7.0        # real governed sandbox backend
# neo4j>=5.0         # optional write-behind graph replica (NEO4J_URI)
